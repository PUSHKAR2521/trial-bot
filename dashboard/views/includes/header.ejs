<head>
    <style>
        a {
            color: white;
            text-decoration: none;
            transition: all .3s ease;
        }

        a:hover {
            color: rgb(0, 225, 255);
        }

        .ham {
            display: none;
        }

        #hammenu {
            display: none;
        }

        #hammenu:checked~.mobilenav {
            display: grid;
            color: white;
        }

        .mobilenav {
            display: none;
        }

        @media only screen and (max-width: 998px) {
            ul {
                position: absolute;
                display: flex;
                align-items: center;
                flex-direction: column;
                top: 70px;
                right: 0;
                background-color: #00FF47;
                width: 100vw;
                z-index: 100;
            }

            .ham {
                display: flex;
            }
        }
    </style>
</head>

<header class="logo" style="display: flex; align-items: center; justify-content: space-between; width: 100%; height: 6%; padding: 0 20px;
background: rgba(255, 255, 255, 0);
    box-shadow: 0 4px 30px rgba(0, 0, 0, 1);
    backdrop-filter: blur(100px);
    -webkit-backdrop-filter: blur(100px);">
    <span><a href="/" style="text-decoration: none; color: #00FF47; font-size: 1.8rem; font-weight: 750;">B A
            C</a></span>

    <ul style="list-style: none; display: flex; gap:5vw; z-index: 1;">
        <li><a href="/selector">Servers</a></li>
        <li><a href="/manage/<%= guild.id %>/basic">Basic</a></li>
        <li><a href="/manage/<%= guild.id %>/greeting">Greeting</a></li>
    </ul>
    <div style="display: flex; align-items: center; justify-content: center; gap: 20px;"><img
            src="<%= user.displayAvatar %>" width="40" height="40" class="user-image" alt="User Image" />
        <a href="/logout" class="btn"><img src="/dist/img/logout.svg"></a>
        <div class="ham">
            <label for="hammenu">
                <svg id="" xmlns="http://www.w3.org/2000/svg" height="48px" viewBox="0 -960 960 960" width="48px"
                    fill="#e8eaed">
                    <path d="M120-240v-60h720v60H120Zm0-210v-60h720v60H120Zm0-210v-60h720v60H120Z" />
                </svg>
            </label>
            <input type="checkbox" id="hammenu">
        </div>
    </div>
</header>

<nav class="mobilenav">
    <a href="/">Home</a>
    <a href="/manage/<%= guild.id %>/basic">Basic</a>
    <a href="/manage/<%= guild.id %>/greeting">Greeting</a>
</nav>